model Payment {
    id        String        @id @default(uuid())
    status    PaymentStatus @default(PENDING)
    amount    Float
    paidAt    DateTime
    updatedAt DateTime      @updatedAt

    @@map("payment")
}
